ansible-lint:
  stage: lint
  image: python:3.12-slim
  tags:
    - docker
  allow_failure: true          # remove once the findings are cleaned up
  before_script:
    - pip install --no-cache-dir ansible-lint
  script:
    - cd ansible
    - ansible-lint