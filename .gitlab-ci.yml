stages:
  - lint

ansible-lint:
  stage: lint
  image: python:3.12-slim
  tags:
    - docker
  allow_failure: true
  before_script:
    - pip install --no-cache-dir ansible-lint==24.9.2
    - ansible-galaxy collection install -r ansible/requirements.yml
  script:
    - ansible-lint ansible/