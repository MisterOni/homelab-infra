# With the compose_stack Ansible role, .env is GENERATED from vault vars.
# GITLAB_ROOT_PASSWORD must be >= 8 chars (GitLab rejects weak initial passwords).
GITLAB_ROOT_PASSWORD=change-me-min-8-chars
