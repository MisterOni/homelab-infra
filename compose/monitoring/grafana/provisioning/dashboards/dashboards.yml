apiVersion: 1
providers:
  - name: homelab
    folder: Homelab
    type: file
    options: { path: /etc/grafana/provisioning/dashboards }
# Drop exported dashboard JSON files (from macbook-backup.sh) in this directory.
